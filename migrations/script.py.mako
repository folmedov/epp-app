"""Alembic script template placeholder.

This project uses the default alembic script template. The file is included
so `alembic` can find a template in the repository if needed by some tools.
"""

from alembic import op
import sqlalchemy as sa
